---
import { assertAffiliateUrl } from '../lib/products';

interface Props {
	href: string;
	label?: string;
	variant?: 'primary' | 'secondary' | 'inline';
	class?: string;
}

const {
	href,
	label = 'Check current price on Amazon',
	variant = 'primary',
	class: className = '',
} = Astro.props;

const safeHref = assertAffiliateUrl(href);

const variants = {
	primary:
		'inline-flex w-full items-center justify-center rounded-xl bg-teal-700 px-4 py-3 text-sm font-bold text-white transition hover:bg-teal-600 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-teal-700 sm:w-auto',
	secondary:
		'inline-flex w-full items-center justify-center rounded-xl border border-teal-700 bg-white px-4 py-3 text-sm font-bold text-teal-800 transition hover:bg-teal-50 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-teal-700 sm:w-auto',
	inline:
		'font-semibold text-teal-700 underline decoration-teal-300 underline-offset-2 transition hover:text-teal-900 hover:decoration-teal-600',
};
---

<a
	href={safeHref}
	target="_blank"
	rel="sponsored nofollow noopener"
	class:list={[variants[variant], className]}
>
	{label}
	{variant !== 'inline' && <span class="sr-only"> (opens in a new tab)</span>}
</a>
